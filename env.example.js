// Copy to app/config.generated.js for local development (generated in CI for Pages deployments).
window.__SPOTIFY_EXPORTER_CONFIG__ = {
  spotifyClientId: "YOUR_SPOTIFY_CLIENT_ID",
  // Optional: only used if you enable "Override redirect URI" in Settings.
  redirectUri: "http://127.0.0.1:5188/",
};
